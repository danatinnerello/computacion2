version: "3.9"

services:
  redis:
    image: redis:7
    container_name: redis

  web:
    build: ./web
    ports:
      - "8000:8000"
    depends_on:
      - redis

  worker:
    build: ./worker
    depends_on:
      - redis